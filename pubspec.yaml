name: dmp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # --- UI & Utility Packages ---
  cupertino_icons: ^1.0.8
  file_picker: ^8.0.0        # For choosing files
  url_launcher: ^6.3.1       # For links, calls, maps, etc.
  pdf: ^3.10.8              # ✅ Add this
  path_provider: ^2.1.4     # ✅ Add this
  share_plus: ^10.0.2 

      # For saving the PDF to device storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  lucide_icons: ^0.257.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/backg.png
    - assets/images/gate.jpg
    - assets/images/office.jpg
    - assets/dmplogofinal.png
